type TopTrendProduct = {
    product_id: number, name: string, price: number, categoryName: string, brandname: string, quantity: number, thumb_img: string;
}


type products = {
    product_id: number, name: string, description: string, price: number, brand_id: number
    , brandname: string, category_id: number, categoryname: string, thumb_img: string
}